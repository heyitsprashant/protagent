# ProtAgent Phase 1 — Dependencies
# Install PyTorch CPU build first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Then install the rest:
#   pip install -r requirements.txt

anthropic>=0.25.0
biopython>=1.83
httpx>=0.27.0
langchain>=0.1.0
langchain-anthropic>=0.1.0
numpy>=1.26.0
python-dotenv>=1.0.0
requests>=2.31.0
transformers>=4.35.0
fastapi>=0.110.0
uvicorn>=0.29.0
pytest>=8.0.0
black>=24.0.0
ruff>=0.3.0